import { WrapResponseInterceptorInterceptor } from './wrap-response-interceptor.interceptor';

describe('WrapResponseInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapResponseInterceptorInterceptor()).toBeDefined();
  });
});
